#column statistics code
#calculates the most expensive shoes (max) in the column
import codecademylib3
import pandas as pd

orders = pd.read_csv('orders.csv')
print(orders.head(10))
most_expensive = orders.price.max()
